<tr>
	<td><%= goal.rank %></td>
	<td>
		<%= goal.name %>
		<span class="delete">
			<%= link_to "delete", goal, method: :delete, confirm: "Are you sure you want to flush your dreams down the toilet?" %>
		</span>
	</td>
	<td><%= goal.cost %></td>
</tr>
